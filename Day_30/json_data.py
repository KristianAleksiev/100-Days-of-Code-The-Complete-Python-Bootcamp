import json

#Write
json.dump()

#Load
json.load()

#Update
json.update()

#with open("generated_passwords.json", mode="r") as passwords_file:
    # json.dump(new_data, passwords_file, indent=4) #=> Writing to a json file, "what", "document name", indentation
    # data = json.load(passwords_file) #Reading the file
    # print(data) #=> Returns dictionary format